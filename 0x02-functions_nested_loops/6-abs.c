#include "main.h"
/**
* _abs - computes the absolute value of an integer
* @n: any given number
* Return: number if positive
* else return number *-1
*/

int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
