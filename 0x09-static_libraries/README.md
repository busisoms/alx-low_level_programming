**0x09-static_libraries**

- `libmy.a` static library containing all the string functions
- `create_static_lib.sh` creates a static library called liball.a from all the .c files that are in the current directory
